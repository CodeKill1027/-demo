server:
  port: 8888

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr:  182.61.53.209:8848
      config:
        server-addr: 182.61.53.209:8848   #去哪里拿配置文件
        file-extension: yaml   #格式


#nacos-config-client-dev.yaml
#${prefix}-${spring.profiles.active}.${file-extension}
  #注意：spring.application.name 在 Nacos 配置管理中会使用 的值来构造部分 dataId。
          #
          #在 Nacos Spring Cloud 中，格式dataId如下：
          #
          #${prefix}-${spring.profiles.active}.${file-extension}
          #的值prefix 是 spring.application.name 默认值。您也可以在 中配置此值spring.cloud.nacos.config.prefix。
        #spring.profiles.active 是当前环境的配置文件。更多详细信息，请参阅 Spring Boot 文档。 注意：当值为spring.profiles.active空时， -会删除对应的连字符，dataId的格式变为： ${prefix}.${file-extension}
        #file-exetension 是配置内容的数据格式，可以配置为spring.cloud.nacos.config.file-extension. 目前仅支持propertiesandyaml 类型。
        group: user_group